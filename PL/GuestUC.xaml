<UserControl x:Class="PL.GuestUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid  VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  FlowDirection="RightToLeft">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" FlowDirection="RightToLeft">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="300"/> 
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="SpLogin" Grid.Column="1" VerticalAlignment="Center" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/> 
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="1.5*"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="tbkEnterMail" Text="התחבר"  Grid.Column="0" Grid.Row="0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15"/>
                <TextBox x:Name="tbxEnterMail"  HorizontalAlignment="Stretch"  Grid.Row="1"   VerticalAlignment="Stretch" />
                <Button x:Name="LogInButton" Content="התחבר"  Grid.Column="1" Grid.Row="1"   VerticalAlignment="Center" HorizontalAlignment="Center"  />
                </Grid>
        </StackPanel>
             
             
            <ComboBox x:Name="cbxfilter" Grid.Column="3" HorizontalAlignment="Right" Width="120"  Margin="0,5,10,0"  VerticalAlignment="Top" Grid.ColumnSpan="2" Visibility="Collapsed">
                <ComboBoxItem Content="יחידות אירוח" />
                <ComboBoxItem Content="דרישות לקוח"/>
                <ComboBoxItem Content="הזמנות"/>

            </ComboBox>

        </Grid>

        <DataGrid Grid.Row="1" x:Name="dataGrid" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" AutoGenerateColumns="True"  FlowDirection="RightToLeft">
             
        </DataGrid>
        <Grid x:Name="UnitsGrid" Grid.Row="1" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
        </Grid>

        <Grid Grid.Row="2"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" FlowDirection="RightToLeft">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="130"/> 
            </Grid.ColumnDefinitions>
            <Button x:Name="AddButton"  Grid.Column="2" Content=" הוסף בקשה חדשה" HorizontalAlignment="Center" Margin="10,5"  VerticalAlignment="Stretch" Width="100" ></Button>
            <Button x:Name="deleteButton"  Grid.Column="1" Content="מחק" HorizontalAlignment="Center" Margin="10,5"  VerticalAlignment="Stretch" Width="100" Visibility="Collapsed" ></Button>

        </Grid>

    </Grid>
</UserControl>
